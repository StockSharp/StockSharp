<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\common.props" />
  <Import Project="$(ConnectorsStubPath)\stub_connectors.props" />
  <PropertyGroup>
    <ProjectGuid>{7D6C7AD7-2735-4BC0-8E37-B21584E43264}</ProjectGuid>
    <RootNamespace>StockSharp.Configuration</RootNamespace>
    <AssemblyName>StockSharp.Configuration.Adapters</AssemblyName>
  </PropertyGroup>

  <Target Name="AddConnProjRefs" BeforeTargets="$(BeforeTargetsInDesignTime)" DependsOnTargets="CreateStubNames">
    <ItemGroup>
      <ProjectReference Include="@(StockSharpNoStubConnectors->'$(ConnectorsGitLabPath)%(identity)\%(identity).csproj')"/>
      <ProjectReference Include="@(StockSharpConnStubsRealNames->'$(ConnectorsGitLabPath)%(identity)\%(identity).csproj')"/>
    </ItemGroup>
  </Target>

  <Target Name="test" DependsOnTargets="AddConnProjRefs">
    <Message Text="@(ProjectReference)" />
  </Target>

</Project>
